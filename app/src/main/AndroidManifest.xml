<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.naturalfisherapp">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_INTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_INTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:requestLegacyExternalStorage="true"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.NaturalFisherApp"
        android:usesCleartextTraffic="true">
        <activity android:name=".view.activities.BodegaPrincipalActivity"></activity>
        <activity android:name=".view.activities.ClientePrincipalActivity" />
        <activity android:name=".view.activities.ConfiguracionActivity" />
        <activity android:name=".view.activities.ProductoPrincipalActivity" />
        <activity android:name=".view.activities.MenuPrincipalActivity" />
        <activity android:name=".view.activities.LoginActivity" />
        <activity android:name=".view.activities.VentaPrinsipalActivity" />
        <activity android:name=".view.activities.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>